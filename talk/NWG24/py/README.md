Figures `figs/xtcartoon.png` and `figs/mapplane.png` were built as follows:

* run `python3 axes.py` to make `xtaxes.png` and `xxaxes.png`
* bring into goodnotes on i-pad and draw figures
* export to `figs/`
