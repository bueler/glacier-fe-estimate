# Notes

The user edits .tex files directly. Before making any edits, run `git diff` (and `git status`) to check for uncommitted changes, so edits are not made on a stale view of the file.

Ask user (=Ed Bueler) if he has the reference locally before blowing tokens on a web search that is not yielding full texts.
